<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/stylesheets/output.css" rel="stylesheet">
    <title>Sign in</title>
</head>
<body>
    <%- include('../partials/toggler', {headerText : '', username : '', breadcrumbs : []}) %>
    <div class="flex min-h-[80vh] items-center justify-center px-4 py-12 sm:px-6 lg:px-8">
        <div class="w-full max-w-md space-y-8">
          <div>
            <h1 class="text-center text-5xl font-bold">TODO</h1>
            <h2 class="mt-6 text-center text-3xl font-bold tracking-tight text-indigo-500">Sign In</h2>
          </div>
          <form class="mt-8 space-y-6" action="/signin" method="POST">
            <div class="rounded-md shadow-lg">
              <div>
                <label for="email-address" class="sr-only">Username</label>
                <input id="email-address" name="username" type="text" autocomplete="name" required class="username relative block w-full rounded-lg border-0 py-4 text-gray-900 ring-1  ring-gray-300 placeholder:text-gray-400 focus:z-10 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6 p-2" placeholder="Email address">
              </div>
                <div class="my-5"></div>
              <div>
                <label for="password" class="sr-only">Password</label>
                <input id="password" name="password" type="password" autocomplete="current-password" required class="relative block w-full rounded-lg border-0 py-4 text-gray-900 ring-1 ring-gray-300 placeholder:text-gray-400 focus:z-10 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6 p-2" placeholder="Password">
              </div>
            </div>
            <div>
              <button type="submit" class="mt-7 group relative flex w-full justify-center rounded-md bg-indigo-600 px-3 py-4 text-sm font-semibold text-white hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">
                Sign In
              </button>
            </div>

            <p class="text-center">Dont have an Account? <a href="/signup" class="underline text-indigo-600">Sign Up here</a></p>
          </form>
        </div>
      </div>

      <% if (errorMessage) { %>
        <div class="alert alert-error shadow-lg absolute bottom-5 max-w-[500px] right-0">
          <div>
            <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
            <span><%= errorMessage %></span>
          </div>
        </div>
      <% } %>

      <%- include('../partials/togglerScript') %>
</body>
</html>
